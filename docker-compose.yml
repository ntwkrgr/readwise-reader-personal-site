services:
  app:
    build: .
    ports:
      - "0.0.0.0:5555:5555"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - cache:/app/.cache
      - ${NOTES_HOST_DIR:-./notes}:/app/notes

volumes:
  cache:
